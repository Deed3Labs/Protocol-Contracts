# @clear/tokens

Color, type scale, spacing and radii, shared by both apps.

Tokens and formatters are the entire shared design surface. Screen-level components stay in the
app that owns them — a tablet counter app and a consumer phone app are different products, with
different density and different vocabulary, and a shared component library would force them to
converge where they should not.
