/**
 * Design tokens shared by the Clear apps: color, type scale, spacing, radii.
 *
 * Tokens and formatters are the entire shared surface. There is no shared component library —
 * a tablet counter app and a consumer phone app are different products, and screen-level
 * components stay in the app that owns them.
 */
export {};
