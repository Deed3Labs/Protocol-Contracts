/**
 * Generated contract artifacts: ABIs, typechain bindings, addresses, read helpers.
 *
 * Ships generated output only. This package must never import Solidity source from
 * `contracts/`, which is AGPL-3.0 — see NOTICE.md at the repo root.
 */
export {};
